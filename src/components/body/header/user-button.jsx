"use client"

import { Button } from "@/components/ui/button"

function UserButton() {
    return (
        <Button variant="ghost" size="main_text">
            Sign in
        </Button>
    )
}

export default UserButton